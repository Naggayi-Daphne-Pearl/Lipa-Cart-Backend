[build]
builder = "NIXPACKS"
preBuildCommand = "npm run predeploy"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "npm run seed && SKIP_BOOTSTRAP_SYNC=true npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
